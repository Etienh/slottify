spring:
  datasource:
    url: jdbc:postgresql://localhost:25432/slottify
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  sql:
    init:
      mode: always

